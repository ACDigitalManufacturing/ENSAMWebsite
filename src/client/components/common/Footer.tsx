import styles from "./styles/footer.module.scss";

function Footer(): JSX.Element {
  return (
    <div className={styles.footerWrapper}>a footer under construction</div>
  );
}

export default Footer;
